<%= form_for [@request.order, @request] do |f| %>
  <% if @request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.text_field :order_id,  hidden: true %>
  <div class="field">
    <%= f.label :requester %><br>
    <%= f.text_field :requester %>
  </div>
  <div class="field">
    <%= f.label "Mensaje: " %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
